{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "outDir": "../out-tsc/app",
    "types": [
      "babylonjs",
      "babylonjs-loaders",
      ""
    ]
  },
  "exclude": [
    "test.ts",
    "**/*.spec.ts"
  ]
}
