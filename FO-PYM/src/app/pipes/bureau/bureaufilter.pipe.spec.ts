import { BureaufilterPipe } from './bureaufilter.pipe';

describe('BureaufilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BureaufilterPipe();
    expect(pipe).toBeTruthy();
  });
});
